services:
  - type: web
    name: real-time-poll-rooms
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HASH_SECRET
        generateValue: true
      - key: DATA_DIR
        value: /tmp/poll-data
